#!/usr/bin/env php
<?php

require 'chirripo-proxy.php';

if (isset($_SERVER['argv'][1])) {
    if ($_SERVER['argv'][1] === '--version') {
        echo 'Chirripo Launcher Version: ' . chirripo_proxy_version() . PHP_EOL;
        exit(0);
    }
}

chirripo_proxy_main();
